function Header() {
  return (
    <header
      style={{
        display: "flex",
        justifyContent: "center",
        backgroundColor: "#005b9f",
        color: "#fff",
        padding: "10px",
        height: "fit-content"
      }}
    >
      <h1 style={{ margin: "0", fonSize: "28px" }}>Geovisualization</h1>
    </header>
  );
}

export default Header;
